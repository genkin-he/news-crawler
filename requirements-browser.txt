# 无头浏览器爬虫专用（crawl_news_browser 部署时用此文件覆盖 requirements.txt）
# 基础依赖与 requirements.txt 一致，并增加浏览器相关

# HTTP 请求
requests>=2.31.0
beautifulsoup4==4.12.3
lxml==5.2.0
curl-cffi>=0.14.0

# Google Cloud
google-cloud-bigquery>=3.13.0
google-cloud-storage>=2.10.0
functions-framework>=3.5.0

# 工具
ftfy==6.3.1
python-dateutil>=2.8.2
pytz>=2023.3
pyyaml>=6.0

# 无头浏览器（版本需与 Dockerfile.firefox 中 playwright install 一致）
playwright==1.58.0
